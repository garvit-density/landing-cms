'use strict';

/**
 * stepper-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::stepper-section.stepper-section');
